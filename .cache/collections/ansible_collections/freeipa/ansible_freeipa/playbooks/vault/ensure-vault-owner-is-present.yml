---
- name: Tests
  hosts: ipaserver
  become: true
  gather_facts: false

  tasks:
  - freeipa.ansible_freeipa.ipavault:
      ipaadmin_password: SomeADMINpassword
      name: symvault
      username: admin
      owners: user01
      ownergroups: ipausers
      action: member
      state: present
